# Agent skills

Per-repo configuration for the engineering skill suite. Each section is a one-line pointer to a doc under `docs/agents/`.

## Issue tracker

Issues live in GitHub Issues at `chonneyhft/claudfolio` (via the `gh` CLI). See [`docs/agents/issue-tracker.md`](./docs/agents/issue-tracker.md).

## Triage labels

Default canonical vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See [`docs/agents/triage-labels.md`](./docs/agents/triage-labels.md).

## Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See [`docs/agents/domain.md`](./docs/agents/domain.md).
